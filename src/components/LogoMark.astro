---
interface Props {
  class?: string;
  title?: string;
}

const { class: className = 'h-11 w-11', title = 'Regresja LBL' } = Astro.props;
---

<img
  class={className}
  src="/logo-brama-swiatla.webp"
  width="512"
  height="512"
  alt={title}
  decoding="async"
/>
